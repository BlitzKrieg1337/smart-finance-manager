# Node/npm dependency summary for this project
# Backend dependencies (see backend/package.json for exact versions):
# axios, bcrypt, bcryptjs, connect-mongo, cors, dotenv, express,
# express-session, jsonwebtoken, lodash, mongodb, mongoose, node-fetch
#
# Frontend dependencies (see frontend/package.json for exact versions):
# react, react-dom, react-router-dom, recharts, axios, canvas-confetti, lucide-react
#
# To install, run inside each folder:
#   cd backend && npm install
#   cd frontend && npm install
#
# Note: This repository does not use Python for the backend. If you need
# Python tooling, create a separate requirements-python.txt and document its purpose.
